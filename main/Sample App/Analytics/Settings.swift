// This enum houses the necessary settings to initialize the Amplitude SDK. The initialization
// is performed in App.swift.
// The Storyteller SDK can be configured to send Analytics data to any provider you choose (we
// have chosen Amplitude as the example here). The code to configure this is shown in the
// StorytellerInstanceDelegate.swift and StorytellerTrackingDelegate classes.

class Settings {
    public static let AmplitudeApiKey = ""
}
